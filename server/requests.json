{
    "options": [
        {"ping":"ping {host} > /dev/null && echo {host} online || echo {host} offline"},
        {"head": "curl {url} -s -I"},
        {"whois": "whois {domain}"},
        {"nslookup": "nslookup {domain} 2>&1"},
        {"ssh_version":"nc {host} -i 3 2> /dev/null"}
    ]
}